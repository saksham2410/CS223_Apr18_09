1.All data files and source files should be exttracted in same folder so that paths need not be changed much
2.Compile and run the file main.cpp
3.Add questions and students in the database if required